/* eslint-disable prettier/prettier */
const INITIAL_STATE = {
  username: '',
  phone: 0,
  password: '',
  confirmpassword: '',
};

const RegisReducer = (state = INITIAL_STATE, action) => {
  const info = state;
  return info;
};
export default RegisReducer;
